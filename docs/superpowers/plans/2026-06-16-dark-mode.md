# Dark Mode Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar suporte a tema escuro (claro / escuro / sistema) ao DS Maré via `next-themes`, com toggle no `SettingsDialog` e tokens CSS invertidos em `.dark {}`.

**Architecture:** `next-themes` injeta um script inline no `<head>` que aplica `class="dark"` no `<html>` antes do primeiro paint, eliminando flash de tema errado. O bloco `.dark {}` no `globals.css` redefine todas as CSS vars do DS — nenhum componente precisa de `dark:` classes individuais. O toggle no `SettingsDialog` usa `useTheme()` para ler e setar `'light' | 'dark' | 'system'`.

**Tech Stack:** Next.js 16, React 19, Tailwind CSS (darkMode: ['class']), next-themes, TypeScript

## Global Constraints

- `darkMode: ['class']` já está configurado em `tailwind.config.ts` — não alterar
- Sempre usar `--save-exact` ao instalar pacotes (sem `^` ou `~`)
- Nunca usar `--no-verify` em commits
- Antes de commitar: `npm run lint && npm run format:check && npm run typecheck`
- Dialogs fecham só no `try`, nunca no `catch`
- Fora do escopo: cores hardcoded nos gráficos Recharts

---

## File Map

| Ação | Arquivo | Responsabilidade |
|------|---------|-----------------|
| Criar | `components/providers/ThemeProvider.tsx` | Client component fino que encapsula `NextThemesProvider` |
| Modificar | `app/layout.tsx` | Adiciona `<ThemeProvider>` + `suppressHydrationWarning` no `<html>` |
| Modificar | `app/globals.css` | Adiciona bloco `.dark {}` com todos os tokens invertidos |
| Modificar | `components/settings/SettingsDialog.tsx` | Adiciona seção "Aparência" com `Segment` de 3 opções |

---

## Task 1: Instalar next-themes e criar ThemeProvider

**Files:**
- Create: `components/providers/ThemeProvider.tsx`

**Interfaces:**
- Produces: `ThemeProvider({ children: ReactNode }): JSX.Element` — usado em `app/layout.tsx`

- [ ] **Step 1: Instalar next-themes**

```bash
npm install --save-exact next-themes
```

Verificar que a linha foi adicionada ao `package.json` sem `^`.

- [ ] **Step 2: Criar o ThemeProvider**

Criar `components/providers/ThemeProvider.tsx`:

```tsx
'use client'

import { ThemeProvider as NextThemesProvider } from 'next-themes'

export function ThemeProvider({ children }: { children: React.ReactNode }) {
  return (
    <NextThemesProvider attribute="class" defaultTheme="system" enableSystem>
      {children}
    </NextThemesProvider>
  )
}
```

- [ ] **Step 3: Verificar typecheck**

```bash
npm run typecheck
```

Esperado: sem erros.

- [ ] **Step 4: Commit**

```bash
git add package.json package-lock.json components/providers/ThemeProvider.tsx
git commit -m "feat(theme): install next-themes and create ThemeProvider"
```

---

## Task 2: Adicionar tokens dark ao globals.css

**Files:**
- Modify: `app/globals.css`

**Interfaces:**
- Consumes: nada
- Produces: classe `.dark` no CSS global que inverte todos os tokens do DS Maré

- [ ] **Step 1: Adicionar bloco `.dark {}` ao globals.css**

Adicionar **após** o fechamento do bloco `@layer base { :root { ... } }` (antes de `@layer utilities`):

```css
  .dark {
    /* Background */
    --bg-base:    oklch(14% 0.018 230);
    --bg-surface: oklch(19% 0.016 230);
    --bg-input:   oklch(22% 0.016 230);
    --bg-subtle:  oklch(24% 0.018 228);
    --bg-muted:   oklch(30% 0.018 228);

    /* Text */
    --text-primary:   oklch(94% 0.008 225);
    --text-secondary: oklch(65% 0.014 226);
    --text-tertiary:  oklch(48% 0.012 226);
    --text-inverse:   oklch(12% 0.018 230);

    /* Accent */
    --accent:        oklch(58% 0.14 230);
    --accent-hover:  oklch(52% 0.14 230);
    --accent-subtle: oklch(22% 0.07 228);
    --accent-text:   oklch(78% 0.10 230);

    /* Positive */
    --positive-subtle: oklch(20% 0.07 172);
    --positive-text:   oklch(72% 0.09 172);

    /* Negative */
    --negative-subtle: oklch(20% 0.07 20);
    --negative-text:   oklch(74% 0.10 22);

    /* Warning */
    --warning-subtle: oklch(20% 0.07 75);
    --warning-text:   oklch(76% 0.10 70);

    /* Border */
    --border:        oklch(28% 0.016 228);
    --border-strong: oklch(36% 0.018 228);

    /* Focus rings */
    --ring-accent:   oklch(58% 0.14 230 / 0.20);
    --ring-negative: oklch(54% 0.13 20  / 0.20);

    /* Shadows */
    --shadow-sm: 0 1px 3px oklch(0% 0 0 / 0.25), 0 1px 2px oklch(0% 0 0 / 0.15);
    --shadow-md: 0 4px 12px oklch(0% 0 0 / 0.30), 0 2px 4px oklch(0% 0 0 / 0.20);
    --shadow-lg: 0 12px 32px oklch(0% 0 0 / 0.40), 0 4px 8px oklch(0% 0 0 / 0.25);
  }
```

O bloco `.dark {}` deve ficar **dentro** de `@layer base`, logo após o fechamento do bloco `:root {}`. O arquivo final ficará assim:

```css
@layer base {
  :root {
    /* ... tokens existentes ... */
  }

  .dark {
    /* ... tokens dark acima ... */
  }

  * {
    @apply border-border;
  }

  body {
    background: var(--bg-base);
    color: var(--text-primary);
    -webkit-font-smoothing: antialiased;
  }
}

@layer utilities {
  .bg-bg-input {
    background-color: var(--bg-input);
  }
}
```

- [ ] **Step 2: Verificar lint**

```bash
npm run lint
```

Esperado: 0 warnings, 0 errors.

- [ ] **Step 3: Commit**

```bash
git add app/globals.css
git commit -m "feat(theme): add dark mode tokens to globals.css"
```

---

## Task 3: Integrar ThemeProvider no root layout

**Files:**
- Modify: `app/layout.tsx`

**Interfaces:**
- Consumes: `ThemeProvider` de `components/providers/ThemeProvider.tsx`
- Produces: `<html>` com `suppressHydrationWarning` e children envolvidos pelo `ThemeProvider`

- [ ] **Step 1: Atualizar app/layout.tsx**

Substituir o conteúdo de `app/layout.tsx` por:

```tsx
import { SpeedInsights } from '@vercel/speed-insights/next'
import type { Metadata, Viewport } from 'next'
import { DM_Sans } from 'next/font/google'
import NextTopLoader from 'nextjs-toploader'
import { Toaster } from 'sonner'
import { ThemeProvider } from '@/components/providers/ThemeProvider'
import './globals.css'

const dmSans = DM_Sans({
  subsets: ['latin'],
  weight: ['300', '400', '500', '600'],
  variable: '--font-dm-sans',
})

export const metadata: Metadata = {
  title: 'Maré',
  description: 'Controle financeiro pessoal',
}

export const viewport: Viewport = {
  themeColor: '#1a78c4',
  width: 'device-width',
  initialScale: 1,
  interactiveWidget: 'resizes-content',
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="pt-BR" suppressHydrationWarning>
      <body className={dmSans.className}>
        <ThemeProvider>
          <NextTopLoader color="var(--accent)" height={2} showSpinner={false} />
          {children}
          <Toaster richColors position="top-center" />
          <SpeedInsights />
        </ThemeProvider>
      </body>
    </html>
  )
}
```

> **Por que `suppressHydrationWarning` no `<html>`:** `next-themes` adiciona `class="light"` ou `class="dark"` ao `<html>` via script antes do paint. O servidor renderiza sem a classe; o cliente a adiciona. Sem `suppressHydrationWarning`, o React lança um warning de hidratação inofensivo mas ruidoso.

- [ ] **Step 2: Rodar o dev server e verificar visualmente**

```bash
npm run dev
```

Abrir `http://localhost:3000`. O app deve carregar normalmente. Abrir DevTools → Elements e verificar que `<html>` tem `class="light"` ou `class="dark"` (conforme preferência do sistema). Sem flash de tema ao recarregar.

- [ ] **Step 3: Verificar typecheck e lint**

```bash
npm run typecheck && npm run lint
```

Esperado: 0 erros.

- [ ] **Step 4: Commit**

```bash
git add app/layout.tsx
git commit -m "feat(theme): wire ThemeProvider into root layout"
```

---

## Task 4: Adicionar toggle de tema ao SettingsDialog

**Files:**
- Modify: `components/settings/SettingsDialog.tsx`

**Interfaces:**
- Consumes: `useTheme()` de `next-themes`, `Segment` + `SegmentOption` de `@/components/ui/segment`

- [ ] **Step 1: Atualizar SettingsDialog.tsx**

Substituir o conteúdo de `components/settings/SettingsDialog.tsx` por:

```tsx
'use client'

import { useState, useTransition } from 'react'
import { useRouter } from 'next/navigation'
import { useTheme } from 'next-themes'
import { toast } from 'sonner'
import { RotateCcw, TriangleAlert } from 'lucide-react'
import { Dialog, DialogContent, DialogHeader, DialogTitle } from '@/components/ui/dialog'
import { Drawer, DrawerContent, DrawerHeader, DrawerTitle } from '@/components/ui/drawer'
import { Button } from '@/components/ui/button'
import { Segment, SegmentOption } from '@/components/ui/segment'
import { useMediaQuery } from '@/hooks/use-media-query'
import { resetAccount } from '@/lib/actions/reset-account'

interface SettingsDialogProps {
  open: boolean
  onOpenChange: (open: boolean) => void
}

const THEME_OPTIONS: SegmentOption[] = [
  { value: 'light', label: 'Claro' },
  { value: 'dark', label: 'Escuro' },
  { value: 'system', label: 'Sistema' },
]

function SettingsContent({ onClose }: { onClose: () => void }) {
  const router = useRouter()
  const { theme, setTheme } = useTheme()
  const [isPending, startTransition] = useTransition()
  const [confirming, setConfirming] = useState(false)

  function handleReset() {
    startTransition(async () => {
      try {
        await resetAccount()
        toast.success('Conta resetada com sucesso.')
        onClose()
        router.push('/dashboard')
      } catch {
        toast.error('Erro ao resetar a conta. Tente novamente.')
      }
    })
  }

  return (
    <div className="space-y-6">
      <div>
        <p className="mb-1 text-small font-semibold text-text-primary">Aparência</p>
        <p className="mb-3 text-small text-text-secondary">
          Escolha como o app deve aparecer.
        </p>
        <Segment
          options={THEME_OPTIONS}
          value={theme ?? 'system'}
          onChange={(v) => setTheme(v)}
        />
      </div>

      <div>
        <p className="mb-1 text-small font-semibold text-text-primary">Zona de perigo</p>
        <p className="mb-4 text-small text-text-secondary">
          Apaga todos os dados e restaura a conta ao estado inicial, mantendo as categorias padrão e
          seus orçamentos.
        </p>
        <Button
          variant="danger"
          onClick={() => setConfirming(true)}
          disabled={confirming}
          className="gap-2"
        >
          <RotateCcw className="h-4 w-4" />
          Resetar conta
        </Button>

        {confirming && (
          <div className="border-negative/30 bg-negative/5 mt-4 space-y-3 rounded-lg border p-4">
            <div className="flex gap-2">
              <TriangleAlert className="mt-0.5 h-4 w-4 shrink-0 text-negative" />
              <div>
                <p className="text-small font-semibold text-negative">Ação irreversível</p>
                <p className="mt-1 text-small text-text-secondary">
                  Os seguintes dados serão apagados permanentemente:
                </p>
                <ul className="mt-2 space-y-0.5 text-small text-text-secondary">
                  <li>· Transações e receitas</li>
                  <li>· Gastos fixos e parcelas</li>
                  <li>· Investimentos e metas</li>
                  <li>· Devedores e pessoas</li>
                  <li>· Contas e cartões</li>
                  <li>· Configurações do mês</li>
                </ul>
                <p className="mt-2 text-small text-text-secondary">
                  As categorias padrão e seus orçamentos serão restaurados.
                </p>
              </div>
            </div>
            <div className="flex gap-2">
              <Button
                variant="outline"
                className="flex-1"
                onClick={() => setConfirming(false)}
                disabled={isPending}
              >
                Cancelar
              </Button>
              <Button
                variant="danger"
                className="flex-1"
                onClick={handleReset}
                disabled={isPending}
              >
                {isPending ? 'Resetando...' : 'Confirmar reset'}
              </Button>
            </div>
          </div>
        )}
      </div>
    </div>
  )
}

export function SettingsDialog({ open, onOpenChange }: SettingsDialogProps) {
  const isDesktop = useMediaQuery('(min-width: 1024px)')

  const title = 'Configurações'
  const content = <SettingsContent onClose={() => onOpenChange(false)} />

  if (isDesktop) {
    return (
      <Dialog open={open} onOpenChange={onOpenChange}>
        <DialogContent>
          <DialogHeader>
            <DialogTitle>{title}</DialogTitle>
          </DialogHeader>
          {content}
        </DialogContent>
      </Dialog>
    )
  }

  return (
    <Drawer open={open} onOpenChange={onOpenChange}>
      <DrawerContent>
        <DrawerHeader>
          <DrawerTitle>{title}</DrawerTitle>
        </DrawerHeader>
        <div className="px-4 pb-6">{content}</div>
      </DrawerContent>
    </Drawer>
  )
}
```

- [ ] **Step 2: Verificar visualmente no browser**

Com `npm run dev` rodando:
1. Abrir Configurações (ícone de engrenagem na sidebar ou bottom nav)
2. Verificar que a seção "Aparência" aparece com o `Segment` de 3 opções
3. Clicar em "Escuro" — o app deve mudar para tema escuro imediatamente
4. Clicar em "Claro" — o app retorna ao tema claro
5. Clicar em "Sistema" — o app segue a preferência do SO
6. Recarregar a página — a preferência deve persistir (sem flash)
7. Abrir DevTools → Application → Local Storage → verificar chave `theme` com valor `'light'`, `'dark'` ou `'system'`

- [ ] **Step 3: Verificar suite completa**

```bash
npm run lint && npm run format:check && npm run typecheck && npm test
```

Esperado: 0 erros, todos os testes passam.

- [ ] **Step 4: Commit**

```bash
git add components/settings/SettingsDialog.tsx
git commit -m "feat(theme): add appearance toggle to SettingsDialog"
```

---

## Task 5: Verificação final e build

- [ ] **Step 1: Build de produção**

```bash
npm run build
```

Esperado: build sem erros. Ignorar warnings de `next-themes` sobre `useLayoutEffect` em SSR — são inofensivos e esperados.

- [ ] **Step 2: Verificar critérios de sucesso**

Checklist manual:
- [ ] Sem flash de tema ao carregar (recarregar 3x, inclusive com Ctrl+Shift+R / hard reload)
- [ ] Preferência persiste após fechar e reabrir o browser
- [ ] Opção "Sistema" muda em tempo real se preferência do SO mudar (testar via System Preferences do OS)
- [ ] Todos os tokens do DS mudam com o tema (backgrounds, textos, borders, accents)
- [ ] Build de produção sem erros

- [ ] **Step 3: Commit final se necessário**

```bash
git add -A
git commit -m "chore(theme): final dark mode wiring and verification"
```
